const heartbeatTime = 1000;
const heartbeatTimeout = 2000;
const voteTimeout = 2000;

module.exports = {
    heartbeatTime: heartbeatTime,
    heartbeatTimeout: heartbeatTimeout,
    voteTimeout: voteTimeout
}